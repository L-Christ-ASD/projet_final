apiVersion: v1
kind: Secret
metadata:
  name: dashboard-auth-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  users: {{ .Values.dashboardAuth | b64enc }}  # Directement utiliser la valeur en base64

